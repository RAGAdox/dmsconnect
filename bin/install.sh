#!/bin/bash
#
# install.sh - Installation script for dmsconnect
#
# This script sets up the environment and performs necessary installation steps.
#
pnpm install --frozen-lock --ignore-scripts=false --no-optional